async function isUserNearHromada(userCoords, hromadaId) {
    // TODO:
    // 1. Найти координаты громади по её ID из базы
    // 2. Сравнить с координатами пользователя
    // 3. Проверить, в пределах ли допустимого радиуса (например, 20 км)
    // 4. Пока возвращаем заглушку true

    return true;
}

module.exports = { isUserNearHromada };